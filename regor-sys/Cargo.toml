[package]
name = "regor-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to ARM's regor compiler (Ethos-U NPU backend)"
license = "MIT OR Apache-2.0"
links = "regor"

[build-dependencies]
cc = "1"
